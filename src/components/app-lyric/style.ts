import styled from 'styled-components'

export const ApplyricWrapper = styled.div`
  position: fixed;
  left: 50%;
  transform: translateX(-50%);
  bottom: 58px;
  background-color: rgba(0, 0, 0, 0.7);
  color: #fff;
  .lyric {
    display: block;
    margin: 10px;
  }
`
